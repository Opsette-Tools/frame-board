export type OpsetteShareConfig = {
  appName: string;
  tagline: string;
  url: string;
  logoSrc?: string;
};

export const opsetteShareConfig: OpsetteShareConfig = {
  appName: "Frame Board",
  tagline: "Place before & after photos side by side or in a grid, then export a clean comparison.",
  url: "https://tools.opsette.io/frame-board/",
  logoSrc: "opsette-logo.png",
};
